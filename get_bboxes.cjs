const fs = require('fs');

async function run() {
    // We don't have three.js in node context, so I'll create a small react component that logs it,
    // or we can just do it in the browser!
    // Actually, I can just inject a quick useEffect inside Macbook-16.tsx to log all meshes and their bounding boxes to the console!
}
